addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
      - gcc-7

install:
  - export CXX="g++-7" CC="gcc-7"

before_script:
  - mkdir -p build && cd build
  - cmake ..

script:
  - make
